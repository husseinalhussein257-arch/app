package com.uniquest.backend.enums;

/**
 * Defines the answer structure of a question.
 *
 * MCQ        - multiple choice with four options (A, B, C, D)
 * TRUE_FALSE - binary choice with two options (A = True, B = False)
 */
public enum QuestionFormat {
    MCQ,
    TRUE_FALSE
}
